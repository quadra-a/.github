# quadra-a

Open-source repositories for `quadra-a`.

- `core` — reusable libraries and runtime foundations
- `cli` — TypeScript CLI
- `mcp-server` — MCP server integration surface
- `cli-rs` — Rust CLI implementation
- `agent-examples` — examples and templates

This README is a migration placeholder and should be replaced before public launch.
